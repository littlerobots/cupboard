buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.9.0'
    }
}

allprojects {
    version = VERSION_NAME
    group = GROUP
}

apply plugin: 'android-reporting'