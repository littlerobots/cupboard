<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:artifact="antlib:org.apache.maven.artifact.ant" name="cupboard" default="help">

	<!--
         The local.properties file is created and updated by the 'android' tool.
         It contains the path to the SDK. It should *NOT* be checked into
         Version Control Systems.
    -->

	<property file="local.properties" />

	<!--
         The ant.properties file can be created by you. It is only edited by the
         'android' tool to add properties to it.
         This is the place to change some Ant specific build properties.
         Here are some properties you may want to change/update:

         source.dir
             The name of the source directory. Default is 'src'.
         out.dir
             The name of the output directory. Default is 'bin'.

         For other overridable properties, look at the beginning of the rules
         files in the SDK, at tools/ant/build.xml

         Properties related to the SDK location or the project target should
         be updated using the 'android' tool with the 'update' action.

         This file is an integral part of the build system for your
         application and should be checked into Version Control Systems.
    -->

	<property file="ant.properties" />

	<!--
         The project.properties file is created and updated by the 'android'
         tool, as well as ADT.

         This contains project specific properties such as project target, and library
         dependencies. Lower level build properties are stored in ant.properties
         (or in .classpath for Eclipse projects).

         This file is an integral part of the build system for your
         application and should be checked into Version Control Systems.
    -->

	<loadproperties srcFile="project.properties" />

	<!-- quick check on sdk.dir -->

	<fail message="sdk.dir is missing. Make sure to generate local.properties using &apos;android update project&apos; or to inject it through an env var" unless="sdk.dir" />

	<!--
        Import per project custom build rules if present at the root of the project.
        This is the place to put custom intermediary targets such as:
            -pre-build
            -pre-compile
            -post-compile (This is typically used for code obfuscation.
                           Compiled code location: ${out.classes.absolute.dir}
                           If this is not done in place, override ${out.dex.input.absolute.dir})
            -post-package
            -post-build
            -pre-clean
    -->

	<import file="custom_rules.xml" optional="true" />

	<!--
         Import the actual build file.

         To customize existing targets, there are two options:
         - Customize only one target:
             - copy/paste the target into this file, *before* the
               <import> task.
             - customize it to your needs.
         - Customize the whole content of build.xml
             - copy/paste the content of the rules files (minus the top node)
               into this file, replacing the <import> task.
             - customize to your needs.

         ***********************
         ****** IMPORTANT ******
         ***********************
         In all cases you must update the value of version-tag below to read 'custom' instead of an integer,
         in order to avoid having your file be overridden by tools such as "android update project"
    -->
	<!-- version-tag: custom -->

	<macrodef name="maven">
		<attribute name="options" default="" />
		<attribute name="goal" />
		<attribute name="basedir" />
		<attribute name="resultproperty" default="maven.result" />
		<element name="args" implicit="true" optional="true" />
		<sequential>
			<java classname="org.codehaus.classworlds.Launcher" fork="true" dir="@{basedir}" resultproperty="@{resultproperty}">
				<jvmarg value="-Xmx512m" />
				<classpath>
					<fileset dir="${maven.home}/boot">
						<include name="*.jar" />
					</fileset>
					<fileset dir="${maven.home}/lib">
						<include name="*.jar" />
					</fileset>
				</classpath>
				<sysproperty key="classworlds.conf" value="${maven.home}/bin/m2.conf" />
				<sysproperty key="maven.home" value="${maven.home}" />
				<arg line="--batch-mode @{options} @{goal}" />
			</java>
		</sequential>
	</macrodef>


	<import file="${sdk.dir}/tools/ant/build.xml" />

	<xmlproperty collapseAttributes="true" file="AndroidManifest.xml" prefix="manifest" />

	<property name="maven-staging-repository-url" value="https://oss.sonatype.org/service/local/staging/deploy/maven2" />
	<property name="maven-staging-repository-id" value="cupboard-staging" />
	<property name="maven-artifact-prefix" value="cupboard-${manifest.manifest.android:versionName}" />
	<property name="maven-artifact" value="${maven-artifact-prefix}.jar" />
	<property name="maven-source-artifact" value="${maven-artifact-prefix}-sources.jar" />
	<property name="maven-javadoc-artifact" value="${maven-artifact-prefix}-javadoc.jar" />
	<property name="maven.dist.dir" value="dist/maven" />

	<target name="-post-build">

		<mkdir dir="dist" />

		<echo message="dist/cupboard-${manifest.manifest.android:versionName}.jar" />

		<jar destfile="dist/cupboard-${manifest.manifest.android:versionName}.jar">

			<fileset dir="${basedir}" includes="LICENSE" />

			<fileset dir="${out.classes.absolute.dir}" excludes="${project.app.package.path}/R.class ${project.app.package.path}/R$*.class ${project.app.package.path}/BuildConfig.class" includes="**/*.class" />

			<fileset dir="${source.absolute.dir}" excludes="${android.package.excludes}" />
		</jar>
	</target>

	<target name="-pre-clean">

		<delete dir="dist">
		</delete>
	</target>

	<target name="javadoc" depends="release">

		<mkdir dir="dist/javadoc" />

		<javadoc access="public" classpath="${sdk.dir}/platforms/${target}/android.jar" destdir="dist/javadoc" doctitle="Cupboard" linkoffline="http://d.android.com/reference ${sdk.dir}/docs/reference" source="1.5" sourcepath="src" use="true" verbose="false">
		</javadoc>
	</target>

	<target name="maven-prepare" depends="javadoc">

		<mkdir dir="dist/maven" />

		<jar destfile="dist/maven/${maven-javadoc-artifact}">

			<fileset dir="dist/javadoc" />
		</jar>

		<jar destfile="dist/maven/${maven-source-artifact}">
			<fileset dir="src/" />
		</jar>

		<copy file="bin/classes.jar" tofile="dist/maven/${maven-artifact}" />

		<copy file="pom.xml" tofile="dist/maven/pom.xml">
			<filterset>
				<filter token="VERSION" value="${manifest.manifest.android:versionName}" />
			</filterset>
		</copy>
	</target>

	<target name="maven-staging" depends="maven-prepare">
		<!-- sign and deploy the main artifact -->
		<maven goal="org.apache.maven.plugins:maven-gpg-plugin:1.3:sign-and-deploy-file" basedir="${maven.dist.dir}" options="-Durl=${maven-staging-repository-url} -DrepositoryId=${maven-staging-repository-id} -DpomFile=pom.xml -Dfile=${maven-artifact} -Pgpg">
		</maven>
		<!-- sign and deploy the sources artifact -->
		<maven goal="org.apache.maven.plugins:maven-gpg-plugin:1.3:sign-and-deploy-file" basedir="${maven.dist.dir}" options="-Dclassifier=sources -Durl=${maven-staging-repository-url} -DrepositoryId=${maven-staging-repository-id} -DpomFile=pom.xml -Dfile=${maven-source-artifact} -Pgpg">
		</maven>
		<!-- sign and deploy the javadoc artifact -->
		<maven goal="org.apache.maven.plugins:maven-gpg-plugin:1.3:sign-and-deploy-file" basedir="${maven.dist.dir}" options="-Dclassifier=javadoc -Durl=${maven-staging-repository-url} -DrepositoryId=${maven-staging-repository-id} -DpomFile=pom.xml -Dfile=${maven-javadoc-artifact} -Pgpg">
		</maven>
	</target>
</project>