apply plugin: "android"

android {
    compileSdkVersion 17
    buildToolsVersion "18.0.1"
    defaultConfig {
        versionCode 1
        versionName version
        minSdkVersion 11
        targetSdkVersion 17
    }
}

dependencies {
    compile project(":library")
}